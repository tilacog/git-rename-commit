US-001: Add -n flag and refactor core to support multiple target commits — DONE
- Made `commit` positional arg optional (required_unless_present = "n")
- Added `-n` / `--last` flag with conflicts_with = "commit"
- Refactored rewrite loop to use HashSet<Oid> target set instead of single OID comparison
- For -n mode: collects last N commits from HEAD, all become targets
- For single-commit mode: walks HEAD to target, only target is in target set
- Updated no-match message to "pattern did not match any commit message in the range."
- Added summary line "Rewrote N of M commits." on success
- All 5 existing tests pass, just ci passes

US-002: Add revision range (A..B) syntax support — DONE
- Detect ".." in positional commit arg, split into from/to endpoints
- Resolve range using git2 revwalk with push(to)/hide(from) for standard git range semantics
- from is excluded, to is included
- Build commit chain from HEAD down to the oldest target in the range
- Validate both sides of range are non-empty, range is non-empty, and all targets are ancestors of HEAD
- Refactored main into resolve_targets/resolve_range/resolve_single helpers to satisfy clippy::too_many_lines
- Mutual exclusivity with -n handled by existing clap conflicts_with on the positional arg
- All 5 existing tests pass, just ci passes

US-003: Integration tests for -n flag — DONE
- Added run_rename_last helper for invoking binary with -n flag
- Test: -n 3 rewrites all 3 when all match (last_n_rewrites_all_matching)
- Test: -n 3 rewrites only matching, leaves others unchanged (last_n_rewrites_only_matching)
- Test: -n 1 equivalent to HEAD (last_1_is_equivalent_to_head)
- Test: -n 0 produces error (last_0_produces_error)
- Test: -n + positional commit is error (n_with_positional_commit_is_error)
- Test: no match in -n range exits nonzero (last_n_no_match_exits_nonzero)
- All 11 tests pass (5 existing + 6 new), just ci passes

US-004: Integration tests for revision range syntax — DONE
- Test: range A..B rewrites only commits in range, not outside (range_rewrites_only_commits_in_range)
- Test: range no match exits nonzero with warning (range_no_match_exits_nonzero)
- Test: -n with range is error (n_with_range_is_error)
- Test: <from> commit excluded from range (range_excludes_from_commit)
- All 15 tests pass (5 existing + 6 -n tests + 4 range tests), just ci passes
